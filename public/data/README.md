This directory is for a published snapshot, `series.json`.

The sample chart data lives in `public/fixtures/sample-usage.ndjson`. A fork collector writes `series.json` here. That file is public on GitHub Pages. Do not commit a file that contains a token.

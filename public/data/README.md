This directory is for a published snapshot, `series.json`.

A fork collector writes `series.json` here. That file is public on GitHub Pages. The page does not load the synthetic fixture on boot. Do not commit a file that contains a token.
